'use client'; import {useState} from 'react'; import {api} from '../../lib/api'; export default function Register(){const [v,setV]=useState({name:'',email:'',password:''}),[msg,setMsg]=useState('');return <section className="section"><div className="container"><div className="card form"><h1>Account তৈরি করুন</h1><label>নাম<input value={v.name} onChange={e=>setV({...v,name:e.target.value})}/></label><label>Email<input value={v.email} onChange={e=>setV({...v,email:e.target.value})}/></label><label>Password<input type="password" value={v.password} onChange={e=>setV({...v,password:e.target.value})}/></label><button className="btn" onClick={async()=>{try{await api('/auth/register',{method:'POST',body:JSON.stringify(v)});setMsg('Account তৈরি হয়েছে। এখন login করুন।')}catch(e){setMsg(e.message)}}}>Register</button>{msg&&<p>{msg}</p>}</div></div></section>}
